server {
    listen        18081;
    server_name   localhost;
    root          ./www;

    location / {
        root           ./www;
        index          index.html;
        allowed_methods  GET POST DELETE;
        autoindex      off;
    }
}
